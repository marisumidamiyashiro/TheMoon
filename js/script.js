const swiper = new Swiper('.swiper', {
    // Optional parameters
    direction: 'vertical',
    mousewheel: true,
    loop: true,
    effect: 'fade',

    // If we need pagination
    pagination: {
        el: '.swiper-pagination',
        clickable: true,
        
    },

});